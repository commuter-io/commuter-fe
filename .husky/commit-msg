echo "🔍 커밋 메시지 컨벤션을 확인합니다..."

if ! npx --no-install commitlint --edit "$1"; then
  echo "😠 커밋 메시지 컨벤션을 지켜주세요."
  exit 1
else
  echo "🌟 커밋 메시지 컨벤션이 올바르게 작성되었습니다."
  exit 0
fi
